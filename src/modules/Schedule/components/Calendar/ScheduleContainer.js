import React from 'react';

import Schedule from './components/Schedule';

const ScheduleContainer = (appointments) => {
  return <App appointments={appointments} />;
};

export default ScheduleContainer;